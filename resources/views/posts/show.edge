@layout('main')

@section('content')
    <a href="/posts">Wanna go back!</a>

  <h1>{{ title }}</h1>

  <p>{{ post.title }}</p>
  <p>{{ post.body }}</p>

  <hr>

  <a href="/posts/edit/{{ post.id }}" class="btn btn-dark d-block float-left">Edit</a>

  <form action="/posts/{{ post.id }}?_method=DELETE" method="POST">
    {{ csrfField() }}

    <button type="submit" class="btn btn-danger d-block float-right" >Delete</button>
  </form>
@endsection